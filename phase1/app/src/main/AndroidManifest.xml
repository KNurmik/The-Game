<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.phase1activity">

    <application
        android:name=".Domain.AppManager"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Infrastructure.MazeFinishActivity"></activity>
        <activity android:name=".Infrastructure.ReactionGameActivity" />
        <activity android:name=".Infrastructure.ReactionInstructionsActivity" />
        <activity android:name=".Infrastructure.MazeMenuActivity" />
        <activity android:name=".Infrastructure.MazeGameActivity" />
        <activity android:name=".Infrastructure.LeaderboardActivity" />
        <activity android:name=".Infrastructure.StartActivity" />
        <activity android:name=".Infrastructure.CustomizationActivity" />
        <activity android:name=".Infrastructure.MatchingGameActivity" />
        <activity android:name=".Infrastructure.RegisterActivity" />
        <activity android:name=".Infrastructure.LogInActivity" />
        <activity android:name=".Infrastructure.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>