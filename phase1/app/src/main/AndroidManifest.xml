<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.phase1activity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Levels.ReactionGame.ReactionGameActivity"></activity>
        <activity android:name=".Levels.ReactionGame.ReactionInstructionsActivity" />
        <activity android:name=".presentation.MainMenu.LeaderboardActivity" />
        <activity android:name=".presentation.MainMenu.StartActivity" />
        <activity android:name=".Profile.CustomizationActivity" />
        <activity android:name=".presentation.MainMenu.MainMenuActivity" />
        <activity android:name=".presentation.Register.RegisterActivity" />
        <activity android:name=".presentation.LogIn.LogInActivity" />
        <activity android:name=".presentation.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>